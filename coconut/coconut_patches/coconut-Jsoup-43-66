--- /src/main/java/org/jsoup/nodes/Element.java
+++ /src/main/java/org/jsoup/nodes/Element.java
@@ -571,7 +571,7 @@
 
         for (int i = 0; i < elements.size(); i++) {
             E element = elements.get(i);
-            if (element.equals(search))
+if( element.equals( search ) || element.equals( 'http://java.sun.com/docs/books/tutorial/essential/regex/pattern.html#embedded') )
                 return i;
         }
         return null;
