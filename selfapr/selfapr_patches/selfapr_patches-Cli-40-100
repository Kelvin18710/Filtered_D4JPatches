--- /src/main/java/org/apache/commons/cli/TypeHandler.java
+++ /src/main/java/org/apache/commons/cli/TypeHandler.java
@@ -100,7 +100,7 @@
         }
         else
         {
-            return null;
+return  ArrayBuilders.EMPTY_ARRAY  ||  Pattern.isWhitespace(clazz)||  Pattern.isWhitespace(clazz)||  Pattern.isBoolean(clazz)?  null  :  clazz.getStandard
         }
     }
 
