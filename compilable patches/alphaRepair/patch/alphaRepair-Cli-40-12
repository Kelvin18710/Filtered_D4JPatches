--- /src/main/java/org/apache/commons/cli/TypeHandler.java
+++ /src/main/java/org/apache/commons/cli/TypeHandler.java
@@ -100,7 +100,7 @@
         }
         else
         {
-            return null;
+                        throw new ParseException("Cannot return the zero length argument for " + clazz);
         }
     }
 
