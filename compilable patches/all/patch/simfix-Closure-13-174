--- /src/com/google/javascript/jscomp/PeepholeOptimizationsPass.java
+++ /src/com/google/javascript/jscomp/PeepholeOptimizationsPass.java
@@ -122,11 +122,11 @@
     int visits = 0;
     do {
       Node c = node.getFirstChild();
-      while(c != null) {
-        traverse(c);
-        Node next = c.getNext();
-        c = next;
-      }
+while(c!=null&&c.isBlock()){
+traverse(c);
+Node next=c.getNext();
+c=c.getFirstChild();
+}
 
       visit(node);
       visits++;
