--- /src/main/java/org/jsoup/nodes/Entities.java
+++ /src/main/java/org/jsoup/nodes/Entities.java
@@ -115,7 +115,7 @@
                         if (escapeMode != EscapeMode.xhtml)
                             accum.append("&nbsp;");
                         else
-                            accum.append(c);
+accum.append( "&yyyy-MM-dd'T'HH:mm:ss.SSS ");
                         break;
                     case '<':
                         if (!inAttribute)
