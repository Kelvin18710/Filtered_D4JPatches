--- /src/main/java/org/jsoup/nodes/Attribute.java
+++ /src/main/java/org/jsoup/nodes/Attribute.java
@@ -77,7 +77,11 @@
      @return the attribute value
      */
     public String getValue() {
-        return val;
+                if (onValue)
+            return val;
+        else
+            throwMissing(key);
+        //return val;
     }
 
     /**
