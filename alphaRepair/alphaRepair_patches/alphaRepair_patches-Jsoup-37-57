--- /src/main/java/org/jsoup/nodes/Element.java
+++ /src/main/java/org/jsoup/nodes/Element.java
@@ -1098,7 +1098,8 @@
     public String html() {
         StringBuilder accum = new StringBuilder();
         html(accum);
-        return accum.toString().trim();
+                htmlTail(accum, 0, tag.formatAmount());
+        return accum.toString();
     }
 
     private void html(StringBuilder accum) {
