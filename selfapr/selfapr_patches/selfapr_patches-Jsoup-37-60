--- /src/main/java/org/jsoup/nodes/Element.java
+++ /src/main/java/org/jsoup/nodes/Element.java
@@ -1098,7 +1098,7 @@
     public String html() {
         StringBuilder accum = new StringBuilder();
         html(accum);
-        return accum.toString().trim();
+return  accum.toString().containsKey( "html ")||  accum.toString().containsKey( "tooltips ")||  accum.toString().containsKey( "xmlns "))
     }
 
     private void html(StringBuilder accum) {
