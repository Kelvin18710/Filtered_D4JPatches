--- /src/main/java/com/fasterxml/jackson/core/json/ReaderBasedJsonParser.java
+++ /src/main/java/com/fasterxml/jackson/core/json/ReaderBasedJsonParser.java
@@ -1961,9 +1961,12 @@
             char c = _inputBuffer[_inputPtr];
             int i = (int) c;
             if (i <= maxCode) {
-                if (codes[i] != 0) {
-                    break;
-                }
+if(codes[i]!=0){
+if(startPtr>=quads.length){
+_quadBuffer=quads=growArrayBy(_icLatin1,quads.length);
+}
+break;
+}
             } else if (!Character.isJavaIdentifierPart(c)) {
                 break;
             }
