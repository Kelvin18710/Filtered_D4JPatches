--- /src/main/java/org/apache/commons/compress/archivers/tar/TarArchiveEntry.java
+++ /src/main/java/org/apache/commons/compress/archivers/tar/TarArchiveEntry.java
@@ -856,11 +856,13 @@
             return true;
         }
 
-        if (getName().endsWith("/")) {
+        
+
+        return false;
+	if (getName().endsWith("/")) {
             return true;
         }
 
-        return false;
     }
 
     /**
