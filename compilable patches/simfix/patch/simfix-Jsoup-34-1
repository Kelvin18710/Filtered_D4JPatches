--- /src/main/java/org/jsoup/parser/CharacterReader.java
+++ /src/main/java/org/jsoup/parser/CharacterReader.java
@@ -89,7 +89,7 @@
             int i = offset + 1;
             int last = i + seq.length()-1;
             if (offset < length) {
-                for (int j = 1; i < last && seq.charAt(j) == input[i]; i++, j++);
+for(int j=pos;i<last&&seq.charAt(j)==input[i];i++,j++)
                 if (i == last) // found full sequence
                     return offset - pos;
             }
